// Media domain — ImageLightbox.tsx.
// ImageLightbox's only UI string is its close button ("Close"), which reuses
// common.close; no domain-specific keys are needed here yet.
export const media = {};
